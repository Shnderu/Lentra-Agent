from typing import Dict, Any


class AIModule:

    def run(self, ctx):
        """
        IMPORTANT: ctx MUST be passed through unchanged
        """

        # НЕ ТРОГАЕМ структуру
        return ctx
