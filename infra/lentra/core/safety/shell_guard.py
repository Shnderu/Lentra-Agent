# ============================================================
# SHELL GUARD DISABLED (DUPLICATE LAYER REMOVED)
# ============================================================

class ShellGuard:
    """
    Disabled to avoid double-layer conflicts.
    Execution safety is now handled structurally.
    """

    @staticmethod
    def scan(value: str):
        return True
